# 谐音取名 - 网名生成器

## 1. Concept & Vision

一款纯前端中文名字谐音网名生成器，以「墨」为核心视觉元素，融合水墨书法与现代简约设计。用户输入姓名即可获得古灵精怪的谐音网名，仿佛一位老先生在宣纸上挥毫泼墨，为你赐下网名。

## 2. Design Language

### 美学方向
新中式水墨风格 - 以宣纸质感为底，墨迹为魂，辅以简约现代的交互设计。整体氛围如同一间雅致的书房，既有文人气息又不失实用性。

### 配色方案
- **深色主题**
  - 背景: `#1a1a2e` (墨蓝夜色)
  - 卡片: `#16213e` (深沉墨蓝)
  - 主色: `#e94560` (朱砂红)
  - 强调: `#f5c7a9` (暖玉白)
  - 文字: `#eaeaea` (月白色)
  - 次要文字: `#a0a0a0`

- **浅色主题**
  - 背景: `#faf8f5` (宣纸白)
  - 卡片: `#ffffff`
  - 主色: `#c75555` (暗朱砂)
  - 强调: `#8b5a3c` (古铜色)
  - 文字: `#2d2d2d`
  - 次要文字: `#666666`

### 字体
- 标题: `"ZCOOL XiaoWei", "Noto Serif SC", serif`
- 正文: `"Noto Sans SC", "PingFang SC", sans-serif`
- 英文/数字: `"Crimson Text", serif`

### 动效哲学
- 所有动画均为 `ease-out`，时长 300-400ms
- 卡片悬浮时轻微上浮 + 阴影加深
- 生成结果以打字机效果逐字显示
- 复制成功时按钮有短暂脉冲反馈

## 3. Layout & Structure

### 页面结构
```
┌─────────────────────────────────────┐
│  Header: Logo + 深浅色切换          │
├─────────────────────────────────────┤
│  Hero区域                           │
│  ┌─────────────────────────────┐    │
│  │  主标题 + 副标题            │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│  输入区域                           │
│  ┌─────────────────────────────┐    │
│  │  姓名输入框  │  随机姓名    │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│  分类标签栏                         │
│  [古风] [可爱] [高冷] [情侣]        │
├─────────────────────────────────────┤
│  生成结果展示区                     │
│  ┌──────┐ ┌──────┐ ┌──────┐        │
│  │网名1 │ │网名2 │ │网名3 │        │
│  │ 复制 │ │ 复制 │ │ 复制 │        │
│  └──────┘ └──────┘ └──────┘        │
├─────────────────────────────────────┤
│  Footer: 版权 + 使用说明            │
└─────────────────────────────────────┘
```

### 响应式策略
- 移动端: 单列布局，分类标签可横向滚动
- 平板: 双列网格
- 桌面: 三列网格，最大宽度 900px 居中

## 4. Features & Interactions

### 核心功能

#### 4.1 姓名输入
- 输入框 placeholder: "输入你的名字，如：李白"
- 实时字数显示
- 支持回车键直接触发生成

#### 4.2 随机姓名
- 内置100+常见姓名库
- 点击随机选择姓氏+名字
- 空输入时随机名直接作为输入

#### 4.3 分类切换
- 四个分类: 古风、可爱、高冷、情侣
- 点击分类加载对应词缀库
- 当前分类高亮显示

#### 4.4 网名生成
- 根据姓氏谐音匹配姓氏前缀
- 根据名字谐音匹配名字后缀
- 组合生成10个网名
- 生成动画: 卡片依次淡入

#### 4.5 一键复制
- 点击网名卡片复制
- 按钮状态: 默认 → 复制中 → 已复制(1.5s) → 恢复
- 复制成功显示提示文字

### 交互细节
- 输入框获得焦点时边框变为主色
- 生成按钮: hover 时背景色加深，active 时轻微下沉
- 网名卡片: hover 时上浮 4px，阴影扩大
- 深浅色切换: 图标旋转动画

## 5. Component Inventory

### 5.1 Header
- Logo: 毛笔字「谐」图标 + 文字
- 主题切换: 太阳/月亮图标按钮

### 5.2 Input Section
- 文本输入框 (height: 52px, border-radius: 12px)
- 随机按钮 (secondary style)
- 生成按钮 (primary style, 朱砂红)

### 5.3 Category Tabs
- 横向排列，间距 12px
- 选中状态: 底部边框 + 文字变色
- 可横向滚动 (移动端)

### 5.4 Result Card
- 尺寸: 最小宽度 140px，高度自适应
- 内含: 网名文字 + 复制按钮
- 状态: 空(无结果时显示占位)、正常、悬浮、复制中、已复制

### 5.5 Toast Notification
- 位置: 页面底部居中
- 自动消失: 2秒
- 用于复制成功等提示

## 6. Technical Approach

### 技术栈
- 纯 HTML5 + CSS3 + Vanilla JavaScript
- 无框架依赖，体积小可快速加载
- 兼容 GitHub Pages 静态部署

### 文件结构
```
/
├── index.html      # 主页面
├── style.css       # 样式文件
├── app.js          # 逻辑文件
├── sitemap.xml     # SEO站点地图
└── SPEC.md         # 规范文档
```

### 谐音生成算法
```javascript
// 姓氏谐音映射表 (示例)
const surnameMap = {
  '李': ['梨', '璃', '鲤', '礼', '黎'],
  '王': ['汪', '忘', '枉', '往', '望'],
  '张': ['樟', '獐', '彰', '涨', '掌'],
  // ... 更多映射
};

// 分类词缀库
const suffixLib = {
  '古风': ['仙', '尊', '墨', '寒', '烟', '霜', '月', '云'],
  '可爱': ['糖', '糖', '豆', '宝', '团', '蜜', '崽', '兔'],
  '高冷': ['孤', '独', '凉', '默', '寂', '冷', '冰', '夜'],
  '情侣': ['伴', '侣', '双', '对', '执', '守', '念', '思'],
};
```

### SEO策略
- 每页独立 title
- 每页独立 meta description
- 每页独立 keywords
- 语义化 HTML 标签
- 友好锚文本链接
- sitemap.xml 提供搜索引擎抓取指引

### 扩展性设计
- 词库独立为对象结构，方便后续追加
- 支持 JSON 格式导入扩展
- 分类可自由添加删除
